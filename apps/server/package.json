{
  "name": "@steelmind/server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@steelmind/brain-engine": "workspace:*",
    "@steelmind/game-engine": "workspace:*",
    "@steelmind/game-types": "workspace:*",
    "hono": "^4.7.0",
    "zod": "^4.0.0"
  }
}
