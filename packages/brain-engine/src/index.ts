// Brain validation, trigger evaluation, directive priority, keyword detection,
// LLM context creation and provider-independent decision handling.
// Never renders combat.

export const PACKAGE_NAME = "@steelmind/brain-engine" as const;
